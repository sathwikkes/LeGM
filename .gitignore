# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/

# Local data (SQLite, saved drafts). data/raw (the nba_api cache) is committed on purpose so
# `legm ingest` can run anywhere, including inside the deployed container, with no NBA requests.
data/*.db
data/drafts/

# Secrets and local config
.env
.env.*
!.env.example
web/.env.local

# Node / Next.js
web/node_modules/
web/.next/
web/out/
web/*.tsbuildinfo
web/next-env.d.ts

# OS / editor
.DS_Store
*.log
.idea/
.vscode/

# Claude Code
.claude/
CLAUDE.md
